#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int W = 80;   
    int H = 20;   
    double ymin = -1.0, ymax = 1.0;

    
    char grid[H + 1][W + 1];

    cout<<"iba";
    for (int i = 0; i <= H; ++i)
        for (int j = 0; j <= W; ++j)
            grid[i][j] = 'd';

}